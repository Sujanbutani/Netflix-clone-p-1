{
  "name": "netflix-clone-1",
  "version": "1.0.0",
  "description": "```bash PORT=5000 MONGO_URI=your_mongo_uri NODE_ENV=development JWT_SECRET=your_jwt_secre TMDB_API_KEY=your_tmdb_api_key ``` ### Run this app locally",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon src/server.js",
		"start": "nodemon src/server.js",
		"build": "npm install && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "author": "",
  "type": "module",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.7.1",
    "nodemon": "^3.1.7"
  }
}
